/* Payload Admin collapsed-nav / header icon. Sources the shared public brand mark. */
export function Icon() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img src="/brand/icon.png" alt="InTourney" style={{ height: 28, width: 'auto' }} />
  )
}

export default Icon
